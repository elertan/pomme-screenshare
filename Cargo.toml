[package]
name = "pomme-screenshare"
version = "0.1.0"
edition = "2024"
description = "Small GPUI desktop app starter."
license = "MIT"

[dependencies]
gpui = "0.2.2"
image = { version = "0.25.10", default-features = false }

[target.'cfg(target_os = "macos")'.dependencies]
core-video = "0.4.3"
